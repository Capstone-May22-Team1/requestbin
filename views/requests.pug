extends index

block requests
  ul
    each request in requests
      li.request
        ul
          each value, header in request.headers
            li.headers
              p.header
                span #{header}:
                | #{value}
        p.body Body: #{request.body};
